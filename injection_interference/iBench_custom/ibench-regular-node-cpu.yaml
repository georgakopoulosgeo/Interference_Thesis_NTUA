apiVersion: apps/v1
kind: Deployment
metadata:
  name: ibench-cpu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ibench-cpu
  template:
    metadata:
      labels:
        app: ibench-cpu
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nginx
                operator: NotIn
                values: ["true"]
      containers:
      - name: ibench
        image: ibench:latest
        imagePullPolicy: IfNotPresent
        command: ["./cpu","260"]
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"