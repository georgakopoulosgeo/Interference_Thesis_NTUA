apiVersion: batch/v1
kind: Job
metadata:
  name: ibench-cpu
spec:
  template:
    metadata:
      labels:
        app: ibench-cpu
    spec:
      containers:
      - name: ibench
        image: ibench:latest
        imagePullPolicy: IfNotPresent
        command: ["./cpu", "120"]
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
      restartPolicy: Never
      nodeSelector:
        kubernetes.io/hostname: node1   # to be overridden in code
  backoffLimit: 0
