apiVersion: batch/v1
kind: Job
metadata:
  name: ibench-l3
spec:
  template:
    metadata:
      labels:
        app: ibench-l3
    spec:
      containers:
      - name: ibench
        image: ibench:latest
        imagePullPolicy: IfNotPresent
        command: ["./l3", "120"]
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
      restartPolicy: Never
      nodeSelector:
        kubernetes.io/hostname: node1   # to be overridden in code
  backoffLimit: 0
