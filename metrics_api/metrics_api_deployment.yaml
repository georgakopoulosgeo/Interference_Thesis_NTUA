apiVersion: apps/v1
kind: Deployment
metadata:
  name: pcm-api-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pcm-api
  template:
    metadata:
      labels:
        app: pcm-api
    spec:
      nodeSelector:
        slo_scheduler: "true"
      containers:
        - name: api
          image: pcm-api:v1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          volumeMounts:
            - name: pcm-metrics
              mountPath: /opt/pcm_metrics     # ← same path as used in your Python code
              readOnly: true
      volumes:
        - name: pcm-metrics
          hostPath:
            path: /mnt/pcm_metrics            # ← this is the mounted folder inside the node container
            type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: pcm-api-service
spec:
  selector:
    app: pcm-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30090
  type: NodePort
