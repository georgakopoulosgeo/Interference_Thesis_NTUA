apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort  # Expose on a static port (30000-32767)
  selector:
    app: my-nginx
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080  # Manually assign port 30080